const accountId=12345   //can't changed in future
let accountEmail="abc@gmail.com"
var accountPassword="11223344"
accountCity="Jaipur"
let accountState;   //it's only declared not value initilized 

console.log(accountId);   // instead of doing console.log() for every variable we can do   console.table([write all variable name]);

console.table([accountId,accountEmail,accountPassword,accountCity,accountState])
